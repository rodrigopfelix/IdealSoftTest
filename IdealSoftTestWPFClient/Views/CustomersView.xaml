<Window x:Class="IdealSoftTestWPFClient.Views.CustomersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IdealSoftTestWPFClient.Views"
        xmlns:vm="clr-namespace:IdealSoftTestWPFClient.ViewModels.Customers"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="Clientes"
        Height="450" Width="800">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding InitCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <DockPanel Margin="10">

        <!-- Buttons -->
        <StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Novo" Command="{Binding NewCommand}" Margin="5,0"/>
            <Button Content="Editar" Command="{Binding EditCommand}" Margin="5,0"/>
            <Button Content="Excluir" Command="{Binding DeleteCommand}" Margin="5,0"/>
        </StackPanel>

        <!-- Grid -->
        <DataGrid ItemsSource="{Binding Customers}"
                  SelectedItem="{Binding SelectedCustomer}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome" Binding="{Binding FirstName}" />
                <DataGridTextColumn Header="Sobrenome" Binding="{Binding LastName}" />
                <DataGridTextColumn Header="Telefones" Binding="{Binding OneLinePhones}" />
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
</Window>