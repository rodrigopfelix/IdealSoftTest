<Window x:Class="IdealSoftTestWPFClient.Views.CustomerPhoneEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IdealSoftTestWPFClient.Views"
        mc:Ignorable="d"
        Title="Editor de telefone" Height="200" Width="300">
    <StackPanel Margin="15">
        <TextBlock Text="Número"/>
        <TextBox Text="{Binding Phone.Number, UpdateSourceTrigger=PropertyChanged}"
             Margin="0,0,0,10"/>

        <TextBlock Text="Tipo"/>
        <TextBox Text="{Binding Phone.Type, UpdateSourceTrigger=PropertyChanged}"
             Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Salvar" Command="{Binding SaveCommand}" Margin="5,0"/>
            <Button Content="Cancelar" Command="{Binding CancelCommand}" Margin="5,0"/>
        </StackPanel>
    </StackPanel>
</Window>
